<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.joheri.proyectofinal">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFinal">
        <activity
            android:name=".NuevaBibliotecaActivity"
            android:parentActivityName=".MainActivity"
            android:label="Biblioteca juegos"
            android:exported="true" />
        <activity
            android:name=".DetallesActivity"
            android:label="Detalles juego"
            android:exported="false"
            android:parentActivityName=".NuevaBibliotecaActivity" />
        <activity
            android:name=".MainActivity"
            android:label="Introducir juegos"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>